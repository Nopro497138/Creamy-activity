{
  "name": "pvz-discord-activity",
  "version": "1.0.0",
  "description": "Plants Mayhem — Discord Activity",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "vite build client --outDir ../dist",
    "postinstall": "vite build client --outDir ../dist"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^1.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "vite": "^5.0.0"
  }
}
